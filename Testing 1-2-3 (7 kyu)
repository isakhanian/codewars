def number(lines):
    result = []
    if len(lines) == 0:
      return result
    else:
      index = 0
      while index < len(lines):
        lines[index] = str(index + 1) + ': ' + str(lines[index])
        result.append(lines[index])
        index = index + 1
    return result
